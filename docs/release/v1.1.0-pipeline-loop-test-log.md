# PatentAgent v1.1.0 Pipeline Loop Test Log

## Source Identity

- branch: `fix/code-review-hardening`
- head: `8b4e693`
- worktree: `/Users/leo/Projects/patents_agent`
- dirty state at start: yes
- pre-existing unrelated dirty paths: `backend/app/main.py`, `tests/test_post_draft_review.py`, `.worktrees/t_cd5e347c`

## Loop Objective

- goal: run multi-round pipeline tests, record every issue, and continue until a full round finds no new issue.
- standard: full-process stability and reliability.
- quality target: final official patent draft quality, including evidence support, grantability claim chart, clean official export, and fail-closed export gates.

## Issue Register

| ID | Round | Severity | Finding | Root Cause | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| LOOP-001 | prior loop gate run | high | Cross-run repeatability check reported drift for every golden workflow. | The repeatability signature included run-bound values: `grantability.id` and an artifact-level `official_package_hash` that incorporates source hash material tied to random project/run ids. | fixed | `scripts/v1_api_smoke.py` now compares `official_text_hash` and normalized quality fields instead. |
| LOOP-002 | prior release-smoke review | medium | `scripts/v1_smoke.sh` still ran the API quality gate only once, so release smoke did not enforce the multi-round loop standard. | The shell wrapper did not pass `--repeat-count` to `scripts/v1_api_smoke.py`. | fixed | `scripts/v1_smoke.sh` now defaults `PATENTAGENT_V1_1_REPEAT_COUNT` to `2`. |
| LOOP-003 | 2 | environment | Sandboxed `scripts/v1_smoke.sh` failed during `cargo test --manifest-path src-tauri/Cargo.toml`; `allocates_localhost_port` returned `PermissionDenied` while binding `127.0.0.1:0`. | Current command sandbox blocks localhost socket binding. A direct Python `socket.bind(('127.0.0.1', 0))` failed the same way, and unsandboxed `cargo test --manifest-path src-tauri/Cargo.toml` passed 5/5. | diagnosed | Use escalated execution for Tauri tests in this Codex environment, or set `PATENTAGENT_SKIP_TAURI_SMOKE=1` and run cargo checks separately with localhost bind permission. |
| LOOP-004 | PR worktree full smoke | high | Clean `origin/main` worktree failed during `cargo check --manifest-path src-tauri/Cargo.toml` with `resource path ../build/backend/patentagent-backend doesn't exist`. | `scripts/v1_smoke.sh` invoked Tauri cargo checks without preparing the resource directory required by `src-tauri/tauri.conf.json`; dirty local worktrees could pass only because an old sidecar build artifact already existed. | fixed | `scripts/v1_smoke.sh` now creates a non-overwriting `build/backend/patentagent-backend` placeholder before cargo checks, and `tests/test_tauri_desktop_skeleton.py` locks the behavior. |

## Test Rounds

| Round | Command | Result | New Issues |
| --- | --- | --- | --- |
| 1 | `python3 scripts/v1_api_smoke.py --repeat-count 3 --report-dir /tmp/patentagent-loop-round-1` | pass: 15/15 workflows, 0 repeatability failures; `stability`, `reliability`, and `final_draft_quality` all passed. | none |
| 2 | `PATENTAGENT_SKIP_INSTALL=1 PATENTAGENT_V1_1_REPORT_DIR=/tmp/patentagent-loop-round-2 scripts/v1_smoke.sh` | failed in sandbox during Tauri `cargo test`; backend pytest, API loop gate, frontend tests, frontend build, and `cargo check` passed before the failure. | `LOOP-003` |
| 2a | `cargo test --manifest-path src-tauri/Cargo.toml` with escalated localhost-bind permission | pass: 5/5 Tauri tests. | none |
| 2b | `PATENTAGENT_SKIP_INSTALL=1 PATENTAGENT_V1_1_REPORT_DIR=/tmp/patentagent-loop-round-2-escalated scripts/v1_smoke.sh` with escalated localhost-bind permission | pass: backend pytest 419/419, API loop gate 10/10 with 0 repeatability failures, frontend vitest 130/130, frontend build, `cargo check`, and Tauri tests 5/5. | none |
| 3 | `PATENTAGENT_SKIP_INSTALL=1 PATENTAGENT_V1_1_REPEAT_COUNT=3 PATENTAGENT_V1_1_REPORT_DIR=/tmp/patentagent-loop-round-3 scripts/v1_smoke.sh` with escalated localhost-bind permission | pass: backend pytest 419/419, API loop gate 15/15 with 0 repeatability failures, frontend vitest 130/130, frontend build, `cargo check`, and Tauri tests 5/5. | none |
| PR-1 | `python3 -m pytest tests/test_v1_quality_gate.py tests/test_v1_api_smoke_script.py -q` in `/tmp/patentagent-loop-pr` | pass: 9/9 tests. | none |
| PR-2 | `python3 scripts/v1_api_smoke.py --repeat-count 3 --report-dir /tmp/patentagent-loop-pr-api-round` in `/tmp/patentagent-loop-pr` | pass: 15/15 workflows, 0 repeatability failures. | none |
| PR-3 | `PATENTAGENT_SKIP_INSTALL=1 PATENTAGENT_V1_1_REPORT_DIR=/tmp/patentagent-loop-pr-smoke scripts/v1_smoke.sh` in clean PR worktree | failed during Tauri cargo check because the sidecar resource path was missing. | `LOOP-004` |
| PR-4 | `python3 -m pytest tests/test_tauri_desktop_skeleton.py::test_v1_smoke_prepares_tauri_resource_placeholder_for_clean_worktrees tests/test_tauri_desktop_skeleton.py::test_tauri_smoke_is_the_desktop_release_gate -q` | pass: 2/2 tests. | none |
| PR-5 | `PATENTAGENT_SKIP_INSTALL=1 PATENTAGENT_V1_1_REPORT_DIR=/tmp/patentagent-loop-pr-smoke-2 scripts/v1_smoke.sh` in clean PR worktree with escalated localhost-bind permission | pass: backend pytest 482/482 with 1 skipped, API loop gate 10/10 with 0 repeatability failures, frontend vitest 144/144, frontend build, `cargo check`, and Tauri tests 5/5. | none |

## Current Stop Condition

PR-5 was run after `LOOP-004` was fixed in a clean `origin/main` worktree. It
exercised the full release smoke and found no new issues. Product pipeline
gates, frontend tests/build, and Tauri checks all passed under the permissions
needed for localhost port allocation.
