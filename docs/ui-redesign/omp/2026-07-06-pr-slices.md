---
status: ready-for-slicing
owner: product/frontend
created: 2026-07-06
source_identity:
  branch: codex/grantatlas-readme-branding
  short_sha: ae767161
  worktree: /Users/leo/Projects/patents_agent
  dirty_at_capture: true
related:
  - docs/ui-redesign/omp/2026-07-06-ui-spec.md
  - docs/ui-redesign/omp/2026-07-06-implementation-plan.md
---

# OMP UI Refactor PR Slices

This file defines the PR units OMP should implement and Codex should review. Each slice should be independently reviewable. Do not merge a slice just because tests pass; it must satisfy the product evidence gate too.

## PR-0 - Baseline Safety

Purpose: protect the current dirty worktree before OMP writes code.

Branch:

```text
codex/omp-ui-baseline
```

Scope:

- No product code changes unless explicitly preserving the current seven dirty files.
- Record source identity and the baseline decision.

Decision required:

- If the current dirty changes are intentional, commit them before OMP starts.
- If they are not intentional, exclude them from the OMP branch and keep the current worktree untouched.

Acceptance:

- Clean OMP worktree.
- Baseline tests pass or known failures are documented.
- Codex confirms the implementation branch starts from the intended source.

## PR-1 - Workbench State And Density

Purpose: refine the current workbench without restarting the shell refactor.

Files:

```text
frontend/src/features/workbench/WorkbenchWorkspace.tsx
frontend/src/features/workbench/WorkbenchWorkspace.test.tsx
frontend/src/features/workbench/selectors.ts
frontend/src/features/workbench/selectors.test.ts
frontend/src/styles.css
```

OMP task prompt:

```text
Read AGENTS.md and docs/ui-redesign/omp/2026-07-06-ui-spec.md first.
Implement PR-1 from docs/ui-redesign/omp/2026-07-06-pr-slices.md.
Stay inside the listed files.
Do not restore the removed workbench right rail unless you can prove it answers a distinct required question.
Protect no-project, selected-project, export-ready, export-locked, and busy states.
Run:
  cd frontend && npm test -- features/workbench/WorkbenchWorkspace.test.tsx features/workbench/selectors.test.ts
Report changed files, tests, and desktop/mobile visual checks.
```

Review checks:

- One primary workbench action is visually dominant.
- No raw implementation keys appear.
- No horizontal overflow at mobile width.
- Selectors stay pure.

## PR-2 - Annotated Repair Editor Data Flow

Purpose: make the embedded repair editor trustworthy with real post-draft review data.

Files:

```text
frontend/src/features/documentRepair/AnnotatedRepairTab.tsx
frontend/src/features/documentRepair/DocumentRepairWorkspace.tsx
frontend/src/flow/panels/PostDraftRepairEditor.tsx
frontend/src/flow/panels/PostDraftIssueRail.tsx
frontend/src/flow/panels/DraftRepairInspector.tsx
frontend/src/PostDraftRepairEditor.test.tsx
frontend/src/features/documentRepair/DocumentRepairWorkspace.test.tsx
frontend/src/styles.css
```

OMP task prompt:

```text
Read AGENTS.md, especially the Annotated Repair Editor Regression Gate.
Implement PR-2 from docs/ui-redesign/omp/2026-07-06-pr-slices.md.
Stay inside the listed files.
Do not accept DOM-only evidence.
Ensure a real repair-session payload with non-empty issues and sections renders all three panes.
Run:
  cd frontend && npm test -- PostDraftRepairEditor.test.tsx features/documentRepair/DocumentRepairWorkspace.test.tsx
Report changed files, tests, and data-flow evidence.
```

Review checks:

- Repairable review selection is tied to the current draft hash.
- Loading, empty, stale, and error states are explicit.
- Issue rail scrolls independently.
- Selecting an issue updates draft highlight and inspector.
- Patch apply marks pending revalidation.

## PR-3 - Quality Panel Responsive Controls

Purpose: finish the currently dirty QualityPanel polish with focused tests.

Files:

```text
frontend/src/flow/panels/QualityPanel.tsx
frontend/src/flow/panels/QualityPanel.test.tsx
frontend/src/styles.css
```

OMP task prompt:

```text
Read AGENTS.md and docs/ui-redesign/omp/2026-07-06-ui-spec.md first.
Implement PR-3 from docs/ui-redesign/omp/2026-07-06-pr-slices.md.
Stay inside the listed files.
Do not restyle unrelated guided panels.
Run:
  cd frontend && npm test -- flow/panels/QualityPanel.test.tsx
Report changed files, tests, and mobile control-fit evidence.
```

Review checks:

- `一键接受补强` disabled behavior is correct.
- `onAcceptAllPatches` is called only with a valid completion run id.
- Cards fit without button clipping.
- Existing expert tool routing still works.

## PR-4 - Integrated Shell Visual QA

Purpose: verify the integrated shell and top-level routes after PR-1 through PR-3.

Files:

```text
frontend/src/app/AppRoot.tsx
frontend/src/app/routes.test.tsx
frontend/src/ui/ShellTopbar.tsx
frontend/src/ui/SystemStatusPanel.tsx
frontend/src/features/documentRepair/*
frontend/src/features/workbench/*
frontend/src/styles.css
docs/ui-redesign/evidence/*
```

OMP task prompt:

```text
Read AGENTS.md and docs/ui-redesign/omp/2026-07-06-ui-spec.md first.
Implement PR-4 from docs/ui-redesign/omp/2026-07-06-pr-slices.md.
This is a verification and small-fix slice, not a redesign.
Run:
  cd frontend && npm test -- app/routes.test.tsx features/workbench/WorkbenchWorkspace.test.tsx features/documentRepair/DocumentRepairWorkspace.test.tsx PostDraftRepairEditor.test.tsx flow/panels/QualityPanel.test.tsx
  cd frontend && npm run build
Capture current-source screenshots for desktop and mobile workbench, document repair, annotated repair, quality, and export.
Write evidence under docs/ui-redesign/evidence/.
```

Review checks:

- All seven sidebar destinations work.
- Topbar chip order matches the contract.
- Desktop and mobile screenshots come from the current dev server.
- Evidence file names include date and branch or short SHA.
- No packaged app or DMG evidence uses a stale mounted volume.

## Final Codex Audit

After OMP finishes all slices, Codex performs the final audit:

```bash
git status --short --branch
git diff --stat
cd frontend && npm test -- app/routes.test.tsx features/workbench/WorkbenchWorkspace.test.tsx features/documentRepair/DocumentRepairWorkspace.test.tsx PostDraftRepairEditor.test.tsx flow/panels/QualityPanel.test.tsx
cd frontend && npm run build
```

Additional audit:

- Inspect diffs for scope drift.
- Inspect screenshots for overlap, clipping, unreadable glass, and mobile overflow.
- Verify annotated repair against a real repair-session response if available.
- Confirm docs remain requirements and are not presented as implementation proof.

