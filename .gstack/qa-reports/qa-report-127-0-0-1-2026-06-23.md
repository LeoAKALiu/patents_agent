# QA Report - PatentAgent Dev Server

**Date:** 2026-06-23  
**URL:** http://127.0.0.1:5174  
**Mode:** report-only, diff-aware  
**Branch:** fix/code-review-hardening  
**Source SHA:** f3948e4b  
**Health Score:** 82/100

## Summary

Dev backend and frontend launched successfully. The guided invention confirmation flow renders the new External Deep Research guide on desktop and mobile, with no console errors. The material detail fallback path is reachable but triggers repeated React duplicate-key console errors. No current real project data can positively exercise the annotated repair editor because no post-draft repair session has non-empty issues.

## Screenshots

- `screenshots/initial-desktop.png`
- `screenshots/project-selected-desktop.png`
- `screenshots/project-selected-mobile-annotated.png`
- `screenshots/materials-empty-desktop.png`
- `screenshots/post-draft-review-failed-run.png`

## Issues

### ISSUE-001 - Duplicate React Key Errors On Material Detail Path

**Severity:** Medium  
**Category:** Functional / Console

**Repro:**

1. Open `http://127.0.0.1:5174`.
2. Select project `DMG测试-智能温控杯`.
3. In `发明点确认`, click `查看前置材料详情`.
4. Check browser console errors.

**Observed:**

React logs repeated duplicate-key errors for `cnipa-CNIPA EPUB helper is not configured; set CNIPA_EPUB_SEARCH_SCRIPT to enable live CNIPA search.`

**Expected:**

Material/detail fallback path opens without React key warnings.

**Evidence:**

- Screenshot: `screenshots/materials-empty-desktop.png`
- Console captured via `browse console --errors`

### ISSUE-002 - Annotated Repair Editor Positive Path Blocked By Missing Real Data

**Severity:** High validation gap  
**Category:** Test Data / Workflow Coverage

**Observed:**

Current real data has no post-draft repair session with non-empty issues. The only post-draft review run is failed and returns `issues=0`.

**Expected:**

A project/run exists or can be seeded where:

- repair-session returns non-empty `issues`
- repair-session returns non-empty draft `sections`
- `打开标注式修复编辑器` is enabled
- issue click updates highlight and inspector

**Evidence:**

- Screenshot: `screenshots/post-draft-review-failed-run.png`
- API scan recorded in `.planning/phases/01-frontend-bug-broad-audit/01-RUNTIME-QA.md`

### ISSUE-003 - Installed App Identity Not Proven

**Severity:** Medium validation gap  
**Category:** Packaging / Artifact Identity

**Observed:**

`/Applications/PatentAgent.app` exists at version `1.1.0`, but the current `backend-startup.log` tail points to a temp DMG smoke path instead of proving a fresh `/Applications/PatentAgent.app` launch.

**Expected:**

Installed app verification should capture fresh app path, backend health endpoint, data directory, and startup log tied to `/Applications/PatentAgent.app`.

## Passed Checks

- Dev backend `/api/health` returned ok.
- Vite proxy `/api/health` returned same backend payload.
- Initial app load had no console errors.
- External Deep Research guide rendered on desktop.
- External Deep Research guide collapsed to one-column mobile layout at `390x844`.
- Clipboard failure fallback text appeared when browser clipboard access failed.
- Project metadata API returned persisted empty fields, matching prompt fallback text.
- Material detail path is reachable from invention confirmation.

## Top 3 Things To Fix

1. Fix or triage duplicate React key errors on the material detail/expert tools path.
2. Add or seed real repair-session data so the annotated repair editor can be verified end to end.
3. Run installed app/DMG verification from an identity-bearing artifact before any handoff.

## Baseline

This report was also summarized in `baseline.json`.
